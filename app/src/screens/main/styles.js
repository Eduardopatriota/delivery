import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabAtivo: {
    textAlign: 'center',  
    color: '#ff8033',
    height: 30,
    fontSize: 8
  },

  tabInativo: {
    textAlign: 'center', 
    color: '#4d4d4d',
    height: 30,
    fontSize: 8
  },
});

export default styles;